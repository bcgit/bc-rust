[package]
name = "bouncycastle-core"
version.workspace = true
edition.workspace = true

[features]
default = ["alloc"]
alloc = [
    "bouncycastle-rng/alloc"
]

[dependencies]
bouncycastle-utils.workspace = true

[dev-dependencies]
bouncycastle-rng = { workspace = true, default-features = false }
